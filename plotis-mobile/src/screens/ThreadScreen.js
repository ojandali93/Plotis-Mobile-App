import React from 'react'
import { Text, StyleSheet } from 'react-native'

const ThreadScreen = () => {
  return (
    <Text>This is the thread screen. It will show message threads for the user</Text>
  )
}

const styles = StyleSheet.create({
  header: {
    fontSize: 22
  }
})

export default ThreadScreen